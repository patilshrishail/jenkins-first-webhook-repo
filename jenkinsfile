pipeline {
    agent any
    stages{
        stage("build") {
            steps {
                echo "compiling the java source code"
                sh 'javac hello.java'

            }
        }

        stage('run') {
            steps {
                echo 'Running the compiled java code'
                sh 'java Helloworld'
            }
        }
    }

}